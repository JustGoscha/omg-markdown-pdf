# Deeper notes

A third file reached via BFS — proves the walker follows links transitively.

```go
package main

import "fmt"

func main() {
    fmt.Println("hello from Go")
}
```

Links back to the [top-level sample](../sample.md) resolve to an internal PDF anchor too.
